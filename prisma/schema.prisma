generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model genders {
  id     Int    @id @default(autoincrement())
  gender String @unique
}
